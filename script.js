// Write a loop that makes seven calls to console.log to output a triangle:

// Triangle piece
let hash = "#";

// Add pieces to build the triangle
for (let i = 0; i < 7; i++) {
    console.log(hash);
    hash += "#";
}